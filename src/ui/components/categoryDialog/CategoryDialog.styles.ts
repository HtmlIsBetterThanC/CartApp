import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  content: {
    gap: 15,
  },
  titleInput: {
    marginBottom: 16,
  },

  actionButtons: {
    flex: 0.5,
    flexDirection: 'row',
    justifyContent: 'space-around',
  },
});
