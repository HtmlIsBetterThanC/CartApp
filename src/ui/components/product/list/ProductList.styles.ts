import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  itemSeparator: {
    height: 16,
  },

  emptyList: {
    fontSize: 24,
    paddingVertical: 16,
    textAlign: 'center',
  },

  footer: {
    height: 90,
  },
});
